# Инструкция по деплою VShu Task Bot

## Требования

- Docker и Docker Compose установлены на сервере
- Telegram Bot Token (получить у [@BotFather](https://t.me/BotFather))

## Быстрый старт

### 1. Клонируйте репозиторий на сервер

```bash
git clone <your-repo-url> vshu_task_bot
cd vshu_task_bot
```

### 2. Создайте файл `.env`

```bash
cp env.example .env
nano .env
```

Заполните переменные:

```env
# ОБЯЗАТЕЛЬНО заполнить:
BOT_TOKEN=123456789:ABCdefGHIjklMNOpqrsTUVwxyz  # Токен от BotFather
POSTGRES_PASSWORD=your_secure_password_here      # Придумайте надёжный пароль

# Опционально (можно оставить по умолчанию):
POSTGRES_USER=vshu_bot
POSTGRES_DB=vshu_bot_db
REMINDER_HOUR=9           # Час отправки напоминаний (по МСК)
REMINDER_MINUTE=0         # Минута отправки
REMINDER_DAYS_BEFORE=3    # За сколько дней до дедлайна напоминать
LOG_LEVEL=INFO            # Уровень логирования (DEBUG, INFO, WARNING, ERROR)
```

### 3. Запустите бота

```bash
docker-compose up -d --build
```

Готово! 🎉

## Полезные команды

### Просмотр логов

```bash
# Логи бота в реальном времени
docker-compose logs -f bot

# Логи базы данных
docker-compose logs -f db

# Все логи
docker-compose logs -f
```

### Логи из файла

```bash
# Все логи
tail -f logs/bot.log

# Только ошибки
tail -f logs/errors.log
```

### Управление контейнерами

```bash
# Остановить
docker-compose down

# Перезапустить
docker-compose restart

# Пересобрать и запустить (после обновления кода)
docker-compose up -d --build

# Полная пересборка без кеша
docker-compose build --no-cache && docker-compose up -d
```

### Обновление бота

```bash
git pull
docker-compose up -d --build
```

## Структура проекта

```
vshu_task_bot/
├── bot/                    # Код бота
│   ├── handlers/           # Обработчики команд
│   ├── keyboards/          # Клавиатуры
│   ├── services/           # Сервисы (планировщик, уведомления)
│   ├── states/             # FSM состояния
│   ├── config.py           # Конфигурация
│   └── main.py             # Точка входа бота
├── database/               # Работа с БД
│   ├── models.py           # Модели данных
│   ├── connection.py       # Подключение к БД
│   └── repositories/       # Репозитории
├── alembic/                # Миграции БД
├── logs/                   # Логи (создаётся автоматически)
├── docker-compose.yml      # Docker конфигурация
├── Dockerfile              # Образ бота
├── requirements.txt        # Python зависимости
├── env.example             # Пример .env файла
└── main.py                 # Главный файл запуска
```

## Функционал бота

### Команды
- `/start` - Начать работу с ботом
- `/menu` - Главное меню
- `/help` - Справка
- `/myprojects` - Мои проекты
- `/mytasks` - Мои задачи

### Роли в проекте
- 🎯 **Проектник** - руководитель проекта (1 на проект)
- ⭐ **Главный организатор** - (макс. 2 на проект)
- 🔧 **Старший ТП** - (1 на проект)
- 📢 **Старший PR** - (1 на проект)
- 📝 **Старший наполнения** - (1 на проект)
- 👤 **Участник** - без ограничений

### Статусы задач
- ⏳ Ожидает
- 🔄 В работе
- ✅ Выполнено
- ⚠️ Задерживается
- ❌ Не выполнено

## Решение проблем

### Бот не запускается

1. Проверьте логи:
```bash
docker-compose logs bot
```

2. Убедитесь, что `.env` файл создан и заполнен

3. Проверьте токен бота

### База данных не подключается

1. Проверьте, что контейнер БД запущен:
```bash
docker-compose ps
```

2. Подождите ~10 секунд после первого запуска (БД инициализируется)

### Миграции не применяются

Миграции применяются автоматически при старте бота. Если возникли проблемы:

```bash
# Зайти в контейнер бота
docker-compose exec bot bash

# Запустить миграции вручную
alembic upgrade head
```

## Резервное копирование

### Создать бэкап БД

```bash
docker-compose exec db pg_dump -U vshu_bot vshu_bot_db > backup_$(date +%Y%m%d_%H%M%S).sql
```

### Восстановить из бэкапа

```bash
cat backup.sql | docker-compose exec -T db psql -U vshu_bot vshu_bot_db
```

## Контакты

При возникновении проблем обращайтесь к разработчику.

